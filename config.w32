// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("DeepTrace", "for DeepTrace support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("DeepTrace", "enable DeepTrace support", "no");

if (PHP_DEEPTRACE != "no") {
	EXTENSION("DeepTrace", "DeepTrace.c");
}

